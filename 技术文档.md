# 禹泽数字科技综合管理系统 — 技术文档

> 电商小程序后端 API · 版本 1.0.0  
> 最后更新：2026-05-17

---

## 1. 项目简介

本项目为**禹泽数字科技综合管理系统**的后端服务，面向微信小程序电商场景，提供用户、商品、订单、财务分账、微信支付进件、线下收款等一体化能力。系统采用 **FastAPI** 构建 RESTful API，使用 **MySQL** 持久化数据，通过 **PyMySQL** 直接执行 SQL（无 ORM），支持微信支付 V3、小程序登录、物流发货管理及多级分销/补贴等业务规则。

**典型使用方**：小程序前端、商家后台、平台运营后台。

**在线 API 文档**（服务启动后）：

| 文档类型 | 地址 |
|---------|------|
| Swagger UI | `http://<host>:<port>/docs` |
| ReDoc | `http://<host>:<port>/redoc` |
| OpenAPI JSON | `http://<host>:<port>/openapi.json` |

默认端口由环境变量 `UVICORN_PORT` 控制，未设置时为 `8000`。

---

## 2. 技术栈

### 2.1 核心框架与运行时

| 类别 | 技术 | 版本要求 | 用途 |
|------|------|----------|------|
| 语言 | Python | ≥ 3.13 | 运行时 |
| Web 框架 | FastAPI | ≥ 0.123.9 | HTTP API、依赖注入、OpenAPI |
| ASGI 服务器 | Uvicorn | ≥ 0.38.0 | 生产/开发服务进程 |
| 数据校验 | Pydantic | ≥ 2.12.5 | 请求/响应模型 |
| 配置 | python-dotenv、pydantic-settings | — | `.env` 与环境变量管理 |
| 包管理 | uv | — | 依赖安装与锁定（`pyproject.toml` / `uv.lock`） |

### 2.2 数据与缓存

| 技术 | 用途 |
|------|------|
| MySQL | 主业务数据库 |
| PyMySQL | 数据库连接、DictCursor、事务 |
| Redis | ≥ 7.1.0，可选缓存/限流等（见依赖） |

> 项目**不使用 SQLAlchemy**，所有数据访问为手写 SQL + `core/database.py` 统一连接管理。

### 2.3 安全与认证

| 技术 | 用途 |
|------|------|
| PyJWT | JWT 签发与校验（微信登录、生产认证） |
| bcrypt | 用户密码哈希 |
| cryptography / pycryptodome | 敏感字段加密（如银行卡） |
| HTTP Bearer | `core/auth.py` 统一鉴权（JWT / UUID 开发模式 / 微信专用 Token） |

### 2.4 微信与支付生态

| 技术 | 用途 |
|------|------|
| wechatpayv3 | 微信支付 API V3（下单、退款、回调） |
| requests / httpx | 调用微信开放平台、进件、物流等 HTTP 接口 |
| xmltodict | 支付/消息 XML 回调解析 |
| qrcode[pil]、Pillow | 线下收款普通二维码生成 |

### 2.5 任务调度与工具库

| 技术 | 用途 |
|------|------|
| APScheduler | 后台定时任务（补贴、进件轮询、订单同步等） |
| openpyxl | 报表/数据导出 |
| pypinyin | 商品名称拼音检索补全 |
| python-multipart | 文件上传 |

### 2.6 开发与质量

| 技术 | 用途 |
|------|------|
| pytest | 单元测试（`tests/`） |

---

## 3. 系统架构

### 3.1 分层设计

```
┌─────────────────────────────────────────────────────────┐
│  表现层   api/          路由、参数校验、HTTP 响应          │
├─────────────────────────────────────────────────────────┤
│  业务层   services/     订单结算、用户、财务、微信等业务    │
├─────────────────────────────────────────────────────────┤
│  模型层   models/schemas/  Pydantic 请求/响应结构        │
├─────────────────────────────────────────────────────────┤
│  基础设施 core/         配置、数据库、认证、中间件、调度   │
└─────────────────────────────────────────────────────────┘
                              │
                              ▼
                        MySQL 数据库
```

### 3.2 应用入口与启动流程

入口文件：`main.py`

1. 加载日志（`core/logging.py`）
2. 创建 `FastAPI` 实例，注册全局异常与 `DecimalJSONResponse`
3. 挂载静态资源：`/pic`（商品图）、`/pic/avatars`（头像）、`/offline`（线下 H5）
4. 注册各模块路由（财务、用户、订单、商品、微信等）
5. **startup** 事件：生产安全配置校验 → 数据库表幂等初始化 → 启动 APScheduler 定时任务

### 3.3 数据访问模式

- **连接**：`core/database.py` 的 `get_conn()` 上下文管理器，自动提交/回滚
- **适配器**：`core/db_adapter.py` 的 `PyMySQLAdapter`，支持命名参数 SQL
- **动态表访问**：`core/table_access.py` 按表结构动态构造 SELECT，数值字段 `COALESCE` 默认值，适应表结构演进

---

## 4. 功能模块

以下按业务域归纳已实现能力（接口数量约 **200+**，以 Swagger 为准）。

### 4.1 用户中心

| 功能 | 说明 |
|------|------|
| 注册 / 登录 | 手机号密码、一键登录、微信小程序 `code` 换 `openid` 登录 |
| 资料管理 | 昵称、头像、密码修改；用户自助注销 |
| 账号状态 | 冻结、解冻、后台重置密码、会员星级调整 |
| 推荐关系 | 绑定推荐人、直推列表、团队递归列表（最多 6 层） |
| 收货地址 | 增删改查、默认地址、商家退货地址 |
| 积分 | 积分增减、余额、流水（用户积分 / 商家积分） |
| 团队奖励 | 按用户、按订单查询待审/已发奖励 |
| 荣誉董事 | 晋升判定、分红明细、董事列表 |

### 4.2 商品管理

| 功能 | 说明 |
|------|------|
| 商品 CRUD | 创建、更新、详情、列表、分类筛选 |
| 搜索 | 名称 / 拼音检索 |
| SKU 与属性 | 多规格、库存、会员商品标识 |
| 轮播图 | Banner 列表维护 |
| 图片 | 商品图、详情图上传；静态访问 `/pic` |
| 销售统计 | 销售数据统计接口 |

### 4.3 订单系统

| 功能 | 说明 |
|------|------|
| 购物车 | 按用户/SKU 管理数量、规格、选中状态 |
| 下单 | 创建订单、积分抵扣、配送方式（平台配送 / 自提） |
| 订单生命周期 | 待支付 → 待发货 → 待收货 → 完成；超时未支付过期 |
| 支付 | 对接微信支付 JSAPI；支付回调处理 |
| 退款 | 申请、审核、与微信退款联动 |
| 商家后台 | 订单列表、发货（快递单号）、退款审核、余额与提现 |
| 物流 | 微信发货管理、快递公司列表、扫码解析运单等 |

### 4.4 财务系统

| 功能 | 说明 |
|------|------|
| 订单结算 | 会员单/普通单分账；多资金池按比例分配（公益、补贴池、平台、荣誉董事等） |
| 积分与优惠券 | 下单抵扣、奖励发券、过期券结算入补贴池 |
| 补贴 | 日补贴自动发放、周补贴、联创月度分红 |
| 提现 | 用户/商家提现申请与审核、税费规则 |
| 公益基金 | 余额查询、流水、报表 |
| 报表 | 财务总览、资金流水、积分流水、积分抵扣明细、交易推荐链等 |
| 奖励审核 | 待审奖励批量通过/拒绝 |

### 4.5 微信支付与进件

| 功能 | 说明 |
|------|------|
| 支付回调 | `/api/wechat-pay` 支付结果通知、订单查询 |
| 服务商进件 | 实名认证、材料上传、进件申请、状态查询 |
| 子商户号回写 | 进件成功后 `wechat_sub_mchid` 写入用户表 |
| Mock 模式 | `WX_MOCK_MODE=true` 本地联调无需真实商户号 |
| 小程序消息 | `/wechat-wxa` 服务器消息推送（发货管理等事件） |

### 4.6 线下收银

| 功能 | 说明 |
|------|------|
| 创建线下单 | 指定商家、金额、生成支付码 |
| 永久收款码 | 小程序码 + 普通二维码 URL + Base64 图 |
| H5 跳转 | `/offline?id=` 落地页跳转小程序；`permanentPay` Universal Link |
| 域名校验 | 根路径及 `offline/` 下微信校验 txt 文件 |
| 支付桥接 | IP 限流、微信内/外浏览器识别 |

### 4.7 银行卡管理

| 功能 | 说明 |
|------|------|
| 绑定 / 解绑 / 改绑 | 验证码校验、卡号加密存储 |
| 风控 | 单日发送次数、同卡绑定人数、IP/用户频率限制 |

### 4.8 店铺设置

| 功能 | 说明 |
|------|------|
| 店铺信息 | 创建、更新、查询 |
| LOGO 上传 | 店铺形象资源 |
| 进件状态 | 与微信进件流程联动查询 |

### 4.9 系统配置

| 功能 | 说明 |
|------|------|
| 系统标语 | 轮播图文案、全局标语的读取与更新 |

---

## 5. 后台定时任务

由 `core/scheduler.py` + `database_setup.start_background_tasks()` 驱动，多进程部署时通过文件锁避免重复执行（Linux `/tmp/scheduler.lock`）。

| 任务 | 调度策略 | 说明 |
|------|----------|------|
| 同步微信订单状态 | 每天 02:00 | 与微信支付侧对账 |
| 清理过期进件草稿 | 每天 04:00 | `DRAFT_EXPIRE_DAYS` |
| 轮询进件审核状态 | 每 10 分钟 | 微信进件状态同步 |
| 进件审核超时检查 | 每天 09:00 | 超 2 个工作日告警 |
| 过期优惠券结算 | 每天 23:58 | 面额入补贴池 + 等额积分 |
| 日补贴发放 | 每天 00:00 | 自动发放日补贴 |
| 联创月度分红 | 每月 1 日 00:00 | 联创等级分红 |
| 清理银行卡验证码 | 每小时 :30 | 过期验证码清理 |

另有订单模块内的**自动收货**等守护逻辑（超时待收货订单标记完成），见 `database_setup` / 订单相关服务。

---

## 6. 数据库设计概要

- 表数量：**约 32 张**（用户、商品、订单、财务账户、流水、积分、优惠券、提现、推荐关系、购物车、退款、进件、线下订单、董事分红等）
- 详细字段说明见：[数据库表结构说明.md](./数据库表结构说明.md)
- 初始化脚本：`database_setup.py`（启动时幂等建表、外键、可选测试数据）

**核心业务表（节选）**：

| 表名 | 职责 |
|------|------|
| `users` | 用户、会员等级、积分、余额、推荐码、子商户号 |
| `products` / `product_skus` | 商品与 SKU 库存价格 |
| `orders` / `order_items` | 订单主表与明细 |
| `finance_accounts` / `account_flow` | 资金池与流水 |
| `cart` | 购物车（支持 SKU 维度） |
| `refunds` | 退款记录 |
| `withdrawals` | 提现申请 |
| `directors` / `director_dividends` | 荣誉董事与分红 |
| `addresses` | 收货/退货地址 |

---

## 7. 第三方集成

| 平台 | 集成内容 |
|------|----------|
| 微信小程序 | 登录、`openid`、小程序码、Url Link、消息推送 |
| 微信支付 V3 | JSAPI 支付、退款、回调验签、服务商/子商户模式 |
| 微信进件 API | 特约商户入驻、审核状态 |
| 微信发货管理 | 物流信息上传、运力公司列表 |

---

## 8. 安全与合规

| 项 | 实现 |
|----|------|
| 认证 | Bearer Token；生产 JWT，开发可开 `ENABLE_UUID_AUTH` |
| 密码 | bcrypt 单向哈希 |
| 银行卡 | 盐值哈希 + 验证码有效期与频率限制 |
| CORS | `core/middleware.py`；生产需配置 `CORS_ALLOW_ORIGINS` |
| 启动校验 | `core/startup_checks.validate_production_safety()` 防止危险配置上线 |
| 支付桥接 | IP 限流（`core/rate_limiter.py`） |
| 环境隔离 | `ENVIRONMENT=development \| testing \| production` |

---

## 9. 项目目录结构

```
DS/
├── api/                    # HTTP 路由（按业务分子包）
│   ├── finance/            # 财务
│   ├── user/               # 用户
│   ├── order/              # 订单、购物车、退款、商家、物流
│   ├── product/            # 商品
│   ├── system/             # 系统配置
│   ├── store_setup/        # 店铺
│   ├── wechat_pay/         # 微信支付
│   ├── wechat_applyment/   # 进件
│   ├── wechat_wxa/         # 小程序消息
│   ├── bankcard/           # 银行卡
│   └── offline/            # 线下收银
├── core/                   # 配置、DB、认证、中间件、调度、微信支付客户端
├── services/               # 业务逻辑
├── models/schemas/         # Pydantic 模型
├── tests/                  # 单元测试
├── scripts/                # 运维/测试脚本
├── offline/                # 线下 H5 静态页与域名校验文件
├── database_setup.py       # 建库建表与后台任务入口
├── main.py                 # 应用入口
├── pyproject.toml          # 依赖声明
└── .env.example            # 环境变量模板
```

更细的模块与类说明见：[项目结构说明文档.md](./项目结构说明文档.md)

---

## 10. 环境变量与部署

### 10.1 必需配置

复制 `.env.example` 为 `.env`，至少配置：

```env
ENVIRONMENT=development
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=***
MYSQL_DATABASE=***
JWT_SECRET_KEY=***          # 生产环境使用强随机串
CARD_SALT=***               # 银行卡加密盐
UVICORN_PORT=8000
```

微信与支付相关变量见 `.env.example` 全文；本地开发可设 `WX_MOCK_MODE=true`。

### 10.2 启动方式

**Windows（推荐 uv）**：

```powershell
uv venv
uv sync
uv run main.py
```

**Linux（systemd）**：参考 [README.md](./README.md) 中 `ds.service` 说明。

### 10.3 静态与域名

- 商品图片目录由 `core/config.py` 中 `PIC_PATH` 等常量指定
- 线下收款需配置 `HOST=https://your-domain.com` 以生成完整二维码链接
- 微信域名校验文件可放在项目根或 `offline/` 目录

---

## 11. 测试

```bash
uv run pytest tests/
```

现有测试示例：

- `tests/test_payment_amounts.py` — 支付金额换算
- `tests/test_distribution_platform_rate.py` — 分账比例

脚本目录 `scripts/` 含 SQL 注入测试、微信通知测试等辅助工具。

---

## 12. 相关文档索引

| 文档 | 内容 |
|------|------|
| [README.md](./README.md) | 快速启动、离线收款说明 |
| [项目结构说明文档.md](./项目结构说明文档.md) | 目录、类、函数级说明 |
| [数据库表结构说明.md](./数据库表结构说明.md) | 全表字段与索引 |
| Swagger `/docs` | 可调试的在线 API 列表 |

---

## 13. 版本与声明

- **API 标题**：禹泽数字科技综合管理系统 API  
- **OpenAPI 版本**：1.0.0  
- 本项目在 AI 辅助下开发，关键业务逻辑需结合代码与测试验证后用于生产环境。
